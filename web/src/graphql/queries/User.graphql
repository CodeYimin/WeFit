query UserById($id: ID!) {
  userById(id: $id) {
    id
  }
}

query Me {
  me {
    id
    username
  }
}

query GetFriendRequests {
  me {
    incomingFriendRequests {
      id
      username
    }

    outgoingFriendRequests {
      id
      username
    }

    friends {
      id
      username
    }
  }
}
